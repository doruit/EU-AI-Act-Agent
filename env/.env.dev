# Environment variables for the EU AI Act Compliance Agent
# This file is safe to commit to source control.
# Secrets and user-specific values go in .env.dev.user (gitignored).

TEAMSFX_ENV=dev
APP_NAME_SUFFIX=dev

# Teams App ID — populated automatically by the M365 Agents Toolkit during provisioning.
TEAMS_APP_ID=

TEAMS_APP_TENANT_ID=